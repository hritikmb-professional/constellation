name: provenance
description: Trace vulnerability lineage through code
version: "1.0"

tools:
  - orbit_query
  - security_findings
  - git_history

config:
  timeout: 30
  retry_count: 2
  fail_on_error: false

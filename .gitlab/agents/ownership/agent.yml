name: ownership
description: Track code ownership and responsibility
version: "1.0"

tools:
  - orbit_query
  - git_blame
  - codeowners
  - org_chart

config:
  timeout: 30
  retry_count: 1
  fail_on_error: false
